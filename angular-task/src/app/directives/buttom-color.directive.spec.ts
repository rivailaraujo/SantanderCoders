import { ButtomColorDirective } from './buttom-color.directive';

describe('ButtomColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtomColorDirective();
    expect(directive).toBeTruthy();
  });
});
